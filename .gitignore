# macOS
.DS_Store

# Xcode — user-specific
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/

# Xcode — build output
build/
DerivedData/
*.hmap
*.dSYM
*.dSYM.zip
*.ipa

# Swift Package Manager
.build/
.swiftpm/

# NOTE: Package.resolved is intentionally NOT ignored — convention is to
# commit it for apps to lock versions. You have zero remote deps, so it
# will be near-empty (harmless), but keep the habit.
