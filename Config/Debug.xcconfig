#include "Shared.xcconfig"

// Debug-only overrides go here. Xcode's default Debug flags (DEBUG, -Onone) still
// apply from the project level, so nothing is needed yet. Example hook for later:
// SWIFT_ACTIVE_COMPILATION_CONDITIONS = $(inherited) DEBUG
